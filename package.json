{
  "name": "tw-api",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/worker.js --platform=browser --format=esm --minify --sourcemap",
    "deploy": "npx wrangler deploy",
    "dev": "npx wrangler dev",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dompurify": "^3.0.8",
    "jsdom": "^24.0.0",
    "nanoid": "^3.3.7" 
  },
  "devDependencies": {
    "esbuild": "^0.19.0",
    "wrangler": "^3.0.0"
  },
  "module": "./dist/worker.js"
}