{
  "name": "combined-library-worker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=dist/worker.js --platform=browser --format=esm",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cookie": "^0.6.0",
    "nanoid": "^3.3.7"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}